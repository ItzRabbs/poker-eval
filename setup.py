from setuptools import setup

setup(
    name='poker-eval',
    version='1.0.0',
    description='Poker odds evaluator',
    author='Maxim Rabinovich',
    author_email='maxim.rabinovich1@gmail.com',
    packages=['poker-eval'],  # same as name
    install_requires=[],  # external packages as dependencies
)
